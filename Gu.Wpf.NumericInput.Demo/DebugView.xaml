<UserControl x:Class="Gu.Wpf.NumericInput.Demo.DebugView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Gu.Wpf.NumericInput.Demo"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="800">
    <UniformGrid Rows="1">
        <GroupBox VerticalAlignment="Top"
                  AutomationProperties.AutomationId="{x:Static local:AutomationIds.DoubleBoxGroupBox}"
                  Header="DoubleBox">
            <local:DoubleBoxView HorizontalAlignment="Left" />
        </GroupBox>

        <GroupBox VerticalAlignment="Top" Header="DecimalBox">
            <local:DecimalBoxView HorizontalAlignment="Left" />
        </GroupBox>

        <GroupBox VerticalAlignment="Top" Header="IntBox">
            <local:IntBoxView HorizontalAlignment="Left" />
        </GroupBox>

        <GroupBox VerticalAlignment="Top" Header="ShortBox">
            <local:ShortBoxView HorizontalAlignment="Left" />
        </GroupBox>
    </UniformGrid>
</UserControl>
