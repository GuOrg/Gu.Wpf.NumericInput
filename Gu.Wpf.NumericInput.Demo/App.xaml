<Application x:Class="Gu.Wpf.NumericInput.Demo.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:numericInput="clr-namespace:Gu.Wpf.NumericInput;assembly=Gu.Wpf.NumericInput"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="MinWidth" Value="60" />
            <Setter Property="HorizontalAlignment" Value="Left" />
        </Style>

        <Style TargetType="{x:Type TextBox}">
            <Setter Property="MinWidth" Value="60" />
            <Setter Property="HorizontalAlignment" Value="Left" />
        </Style>

        <DataTemplate x:Key="DebugInfoTemplate" DataType="{x:Type numericInput:INumericBox}">
            <StackPanel IsEnabled="False">
                <TextBlock Text="{Binding Text, StringFormat='Text: {0}'}" />
                <TextBlock Text="{Binding Value, StringFormat='Value: {0}'}" />
                <TextBlock Text="{Binding DataContext.Value, StringFormat='Vm_Value: {0}'}" />
            </StackPanel>
        </DataTemplate>
    </Application.Resources>
</Application>
